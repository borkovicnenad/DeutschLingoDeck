diff --git a/backend/build.gradle b/backend/build.gradle
index d790c61..392e0aa 100644
--- a/backend/build.gradle
+++ b/backend/build.gradle
@@ -4,7 +4,7 @@ plugins {
 	id 'io.spring.dependency-management' version '1.1.7'
 }
 
-group = 'com.learning'
+group = 'com.deutschlingodeck'
 version = '0.0.1-SNAPSHOT'
 
 java {
@@ -13,6 +13,11 @@ java {
 	}
 }
 
+ext {
+	set('mapstructVersion', "1.6.3")
+	set('springdocVersion', "3.0.3")
+}
+
 repositories {
 	mavenCentral()
 }
@@ -25,10 +30,14 @@ dependencies {
 	implementation 'org.springframework.boot:spring-boot-starter-validation'
 	implementation 'org.springframework.boot:spring-boot-starter-webmvc'
 	implementation 'org.flywaydb:flyway-database-postgresql'
+	implementation "org.springdoc:springdoc-openapi-starter-webmvc-ui:${springdocVersion}"
+	implementation "org.mapstruct:mapstruct:${mapstructVersion}"
 	compileOnly 'org.projectlombok:lombok'
 	developmentOnly 'org.springframework.boot:spring-boot-devtools'
 	runtimeOnly 'org.postgresql:postgresql'
 	annotationProcessor 'org.projectlombok:lombok'
+	annotationProcessor "org.mapstruct:mapstruct-processor:${mapstructVersion}"
+	annotationProcessor 'org.projectlombok:lombok-mapstruct-binding:0.2.0'
 	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'
 	testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
 	testImplementation 'org.springframework.boot:spring-boot-starter-flyway-test'
diff --git a/backend/src/main/java/com/deutschlingodeck/BackendApplication.java b/backend/src/main/java/com/deutschlingodeck/BackendApplication.java
index 9841292..be081f2 100644
--- a/backend/src/main/java/com/deutschlingodeck/BackendApplication.java
+++ b/backend/src/main/java/com/deutschlingodeck/BackendApplication.java
@@ -1,4 +1,4 @@
-package com.learning.backend;
+package com.deutschlingodeck;
 
 import org.springframework.boot.SpringApplication;
 import org.springframework.boot.autoconfigure.SpringBootApplication;
diff --git a/backend/src/main/resources/application.yaml b/backend/src/main/resources/application.yaml
index 4986124..678dbda 100644
--- a/backend/src/main/resources/application.yaml
+++ b/backend/src/main/resources/application.yaml
@@ -8,4 +8,15 @@ spring:
     driver-class-name: org.postgresql.Driver
   jpa:
     hibernate:
-      ddl-auto: validate
+      # TODO: switch to "validate" once Flyway migrations exist for the
+      # User/Dictionary/Card/Game/GameAnswer entities (STEP 12 of the
+      # backend skeleton explicitly defers Flyway migrations).
+      ddl-auto: none
+
+springdoc:
+  api-docs:
+    path: /v3/api-docs
+  swagger-ui:
+    path: /swagger-ui.html
+    operationsSorter: alpha
+    tagsSorter: alpha
diff --git a/backend/src/test/java/com/deutschlingodeck/BackendApplicationTests.java b/backend/src/test/java/com/deutschlingodeck/BackendApplicationTests.java
index 02f7094..a2b364a 100644
--- a/backend/src/test/java/com/deutschlingodeck/BackendApplicationTests.java
+++ b/backend/src/test/java/com/deutschlingodeck/BackendApplicationTests.java
@@ -1,4 +1,4 @@
-package com.learning.backend;
+package com.deutschlingodeck;
 
 import org.junit.jupiter.api.Test;
 import org.springframework.boot.test.context.SpringBootTest;
diff --git a/diff.txt b/diff.txt
index e36b626..e69de29 100644
--- a/diff.txt
+++ b/diff.txt
@@ -1,73 +0,0 @@
-diff --git a/backend/src/main/resources/application.yaml b/backend/src/main/resources/application.yaml
-index a48c390..4986124 100644
---- a/backend/src/main/resources/application.yaml
-+++ b/backend/src/main/resources/application.yaml
-@@ -1,3 +1,11 @@
- spring:
-   application:
-     name: backend-app
-+  datasource:
-+    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:deutschlingodeck}
-+    username: ${DB_USER:deutschlingodeck}
-+    password: ${DB_PASSWORD:deutschlingodeck}
-+    driver-class-name: org.postgresql.Driver
-+  jpa:
-+    hibernate:
-+      ddl-auto: validate
-diff --git a/docker-compose.yml b/docker-compose.yml
-index e69de29..f164add 100644
---- a/docker-compose.yml
-+++ b/docker-compose.yml
-@@ -0,0 +1,52 @@
-+services:
-+  postgres:
-+    image: postgres:16-alpine
-+    container_name: deutschlingodeck-postgres
-+    environment:
-+      POSTGRES_DB: ${DB_NAME:-deutschlingodeck}
-+      POSTGRES_USER: ${DB_USER:-deutschlingodeck}
-+      POSTGRES_PASSWORD: ${DB_PASSWORD:-deutschlingodeck}
-+    ports:
-+      - "${DB_PORT:-5432}:5432"
-+    volumes:
-+      - postgres-data:/var/lib/postgresql/data
-+    healthcheck:
-+      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-deutschlingodeck} -d ${DB_NAME:-deutschlingodeck}"]
-+      interval: 5s
-+      timeout: 5s
-+      retries: 10
-+
-+  backend:
-+    build:
-+      context: ./backend
-+    container_name: deutschlingodeck-backend
-+    environment:
-+      DB_HOST: postgres
-+      DB_PORT: 5432
-+      DB_NAME: ${DB_NAME:-deutschlingodeck}
-+      DB_USER: ${DB_USER:-deutschlingodeck}
-+      DB_PASSWORD: ${DB_PASSWORD:-deutschlingodeck}
-+    ports:
-+      - "8080:8080"
-+    volumes:
-+      - ./backend:/app
-+      - gradle-cache:/root/.gradle
-+    depends_on:
-+      postgres:
-+        condition: service_healthy
-+
-+  frontend:
-+    build:
-+      context: ./frontend
-+    container_name: deutschlingodeck-frontend
-+    ports:
-+      - "4200:4200"
-+    volumes:
-+      - ./frontend:/app
-+      - /app/node_modules
-+    depends_on:
-+      - backend
-+
-+volumes:
-+  postgres-data:
-+  gradle-cache:
