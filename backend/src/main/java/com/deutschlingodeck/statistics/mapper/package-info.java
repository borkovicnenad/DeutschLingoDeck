/**
 * Reserved for mappers once statistics are computed from aggregation query
 * projections rather than a persisted entity (no {@code statistics} entity
 * exists by design — see {@code statistics.service.StatisticsService}).
 */
package com.deutschlingodeck.statistics.mapper;
